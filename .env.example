# AD-7 — secrets live only in Vercel env vars (Production + Preview).
# This file documents the expected keys; it holds no real values.

# Resend API key (resend.com dashboard).
RESEND_API_KEY=

# Where the notification email lands — Stephane's inbox.
CONTACT_TO_EMAIL=

# Sender address — must be on a domain verified in Resend.
RESEND_FROM_EMAIL=
